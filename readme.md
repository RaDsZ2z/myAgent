# TODO

#### 📦 阶段一：数据健壮性（消除正则依赖）

- [ ] **重构 Prompt 规范：** 废弃 `Action: tool_name(args)` 的文本格式，强迫模型输出标准的 `JSON` 块（例如：`{"tool": "...", "args": {...}}`）。
- [ ] **升级参数解析器：** 编写纯 JSON 解析逻辑，加入 `try-except` 捕获 JSON 格式错误，并在解析失败时自动将报错返回给模型（实现 Self-Correction/自我修复）。
- [ ] **参数强校验：** 引入工具函数的参数检查，确保模型传入的参数名和类型完全对齐，防止运行时奔溃。

#### 🧠 阶段二：上下文控制（防止 Token 爆炸与失忆）

- [ ] **滚动窗口清理（Context Window Sliding）：** 限制输入大模型的历史 ReAct 轮数（如仅保留最近 3 轮的 `Thought-Action-Observation` 细节）。
- [ ] **历史记忆压缩（Memory Summarization）：** 当对话超过一定长度时，触发一个独立的 LLM 任务，将早期的工具调用历史压缩成一段简短的“事实清单（State Summary）”，作为固定上下文置顶。

#### 🛠️ 阶段三：功能与安全扩展

- [ ] **工作空间沙盒化（Workspace Sandboxing）：** 限制所有文件工具的操作路径，绝对禁止 Agent 读写指定项目目录（如 `./workspace/`）之外的任何系统文件。
- [ ] **复合文件工具开发：** 新增一个支持智能代码修改的复合工具（如 `patch_file`），避免每次修改文件都需要大模型重写整个长文本。